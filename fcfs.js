function calcularFCFS(posicaoInicial, sequenciaDeTrilhas) {
    /*
        FCFS = First Come, First Served
        O caminho percorrido é exatamente a ordem em que as requisições chegaram.
        Os cálculos matemáticos de distância já são tratados globalmente pelo script.js.
    */
    
    return [...sequenciaDeTrilhas];
}
